model User {
  id                String             @id @default(uuid()) @db.Uuid
  email             String             @unique
  name              String
  passwordHash      String
  status            UserStatus         @default(PENDING)
  emailConfirmed    Boolean            @default(false)
  roles             UserRole[]         @default([REGISTERED])
  createdAt         DateTime           @default(now())
  characters        Character[]
  emailConfirmation EmailConfirmation?
}

enum UserStatus {
  APPROVED
  REJECTED
  PENDING
  SUSPENDED
}

enum UserRole {
  REGISTERED
  POLICE
  DISPATCH
  CIVILIAN
  ADMIN
}
