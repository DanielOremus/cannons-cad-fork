{
  "name": "cannons-cad",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "backend",
    "frontend",
    "shared"
  ],
  "scripts": {
    "backend:up": "docker compose --env-file ./backend/.env up",
    "backend:down": "docker compose --env-file ./backend/.env down"
  },
  "devDependencies": {
    "prettier": "^3.9.5"
  }
}
